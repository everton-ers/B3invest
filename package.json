{
  "name": "ritainvest",
  "version": "1.0.0",
  "description": "Gerenciador de Carteira da Bolsa de Valores Brasileira",
  "main": "server/index.js",
  "scripts": {
    "install:all": "npm install && npm --prefix client install --legacy-peer-deps",
    "start": "node server/index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server/index.js",
    "client": "npm --prefix client start",
    "build": "npm --prefix client run build",
    "render-build": "npm install && npm --prefix client install --legacy-peer-deps && npm --prefix client run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
